// @ts-nocheck
const className = 'CodeContainer';

export enum EClassNames {
  CODE = `${className}__code`,
  HEADER = `${className}__header`,
  NAV = `${className}__nav`,
  NAV_ITEM = `${className}__nav-item`,
  SEPARATOR = `${className}__separator`,
}
