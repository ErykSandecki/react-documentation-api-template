// @ts-nocheck
const className = 'ModalEndpointFinder';

export enum EClassNames {
  FOOTER = `${className}__footer`,
  INPUT = `${className}__input`,
  INPUT_WRAPPER = `${className}__input-wrapper`,
  INPUT_WRAPPER_ICON = `${className}__input-wrapper-icon`,
  LINK = `${className}__link`,
  LINK_SELECTED = `${className}__link-selected`,
  LIST_WRAPPER = `${className}__list-wrapper`,
  MODAL_CONTAINER = `${className}__modal-container`,
  PROMPT = `${className}__prompt`,
}
