// @ts-nocheck
const className = 'LoaderPage';

export enum EClassNames {
  WRAPPER = `${className}__wrapper`,
  ELEMENT = `${className}__element`,
  FILTER = `${className}__filter`,
  LOGO = `${className}__logo`,
  SPINNER = `${className}__spinner`,
}
