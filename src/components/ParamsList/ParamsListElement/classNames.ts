// @ts-nocheck
const className = 'ParamsListElement';

export enum EClassNames {
  ARROW = `${className}__arrow`,
  PARAM_DESCRIPTION = `${className}__param-description`,
  PARAM_HEADER = `${className}__param-header`,
  PARAM_NAME = `${className}__param-name`,
  PARAM_OPTIONAL = `${className}__param-optional`,
  PARAM_REQUIRED = `${className}__param-required`,
  PARAM_TYPE = `${className}__param-type`,
  TRASH = `${className}__trash`,
}
