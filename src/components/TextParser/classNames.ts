// @ts-nocheck
const className = 'TextParser';

export enum EClassNames {
  BLOCK_TEXT = `${className}__block-text`,
  BRACKET = `${className}__bracket`,
  IGNORE = `${className}__ignore`,
  OBJECTS = `${className}__objects`,
  PRIMITIVE_VARIABLE = `${className}__primitive-variable`,
}
